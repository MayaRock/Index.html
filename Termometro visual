<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Termómetro Visual</title>
  <style>
    :root{
      --tube-width: 80px;
      --tube-height: 320px;
      --bulb-size: 110px;
      --transition: 450ms cubic-bezier(.2,.9,.2,1);
      --level: 0%; /* controla la altura del relleno */
    }

    *{box-sizing:border-box}
    body{
      font-family: Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
      display:flex;
      align-items:center;
      justify-content:center;
      min-height:100vh;
      margin:0;
      background:linear-gradient(180deg,#f7f9fc,#eef3fb);
      color:#0b1220;
      padding:24px;
    }

    .card{
      background:white;
      border-radius:16px;
      padding:20px;
      box-shadow:0 10px 30px rgba(10,20,40,0.08);
      display:grid;
      grid-template-columns: 1fr 250px;
      gap:24px;
      align-items:center;
      max-width:900px;
      width:100%;
    }

    .thermo-wrap{
      display:flex;
      flex-direction:column;
      align-items:center;
      gap:14px;
    }

    /* Termómetro */
    .thermo{
      position:relative;
      width:var(--tube-width);
      height:calc(var(--tube-height) + var(--bulb-size)/2);
      display:flex;
      align-items:flex-end;
      justify-content:center;
    }

    .tube{
      position:relative;
      width:calc(var(--tube-width) * 0.45);
